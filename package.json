{
  "name": "vite-sprockets",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "start": "vite --host 0.0.0.0",
    "build": "yarn build:clean && vite build",
    "build:clean": "rm -rf dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.89.2",
    "vite": "^6.0.0"
  },
  "packageManager": "yarn@4.9.2",
  "dependencies": {
    "crypto": "^1.0.1",
    "vite-plugin-static-copy": "^3.0.2"
  }
}
